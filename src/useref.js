import React from 'react'

const ArrayPrec = () => {

  const arr = [1,3,4,5,6,7];
  const arr2= 
  return (
    <h1> {arr.map((a)=><p>{a.name} {a.id} {a.class}{a.rollNo}x</p>)}</h1>
  
  )
}

export default ArrayPrec;